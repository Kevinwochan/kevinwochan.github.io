# Claude Code Instructions

## Git Workflow

- **Merge directly into `main`** — do not create pull requests or use GitHub PRs.
- After merging Claude branches, delete them (local and remote) and remove their worktrees.
