version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --legacy-peer-deps  
    build:
      commands:
        - gatsby build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
